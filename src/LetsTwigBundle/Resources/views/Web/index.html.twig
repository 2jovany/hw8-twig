<html>
    <head>
        <title>Lets Twig!</title>
    </head>
    <body>
        <header>
            <ul class="head_menu">
            {% for menu_item in menu %}
                <li><a href="{{ menu_item.link }}">{{ menu_item.title }}</a></li>
            {% endfor %}
            </ul>
        </header>
        <section>

        </section>
        <section>

        </section>
        <section>

        </section>
        <footer>
            <ul class="foot_menu">
                {% for foot_menu_item in foot_menu %}
                    <li><a href="{{ foot_menu_item.link }}">{{ foot_menu_item.title }}</a></li>
                {% endfor %}
            </ul>
        </footer>
    </body>
</html>